public void addL(int data){
    Node newNode =new Node(data);
    size++;
    if(head == null){
        head =tail=newNode;
        tail.next =head;
    }
    tail.next =newNode;
    tail =newNode;
    tail.next =head;
   }
